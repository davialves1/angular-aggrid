describe('My First Test', () => {
  it('Visits the initial project page', () => {
    cy.visit('/');
    // cy.contains('Welcome');
    // cy.contains('User login status:');
    // cy.contains('Not logged in');
    // cy.get('button').click();
    // cy.contains('Authenticated');
    // cy.visit('/randomhtml');
    // cy.contains('404');
  });
});
